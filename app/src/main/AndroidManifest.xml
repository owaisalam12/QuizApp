<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oways.quizintents">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:label="@string/app_name" android:name=".question1"/>
        <activity android:label="@string/app_name" android:name=".question2"/>
        <activity android:label="@string/app_name" android:name=".question3"/>
        <activity android:label="@string/app_name" android:name=".question4"/>
        <activity android:label="@string/app_name" android:name=".question5"/>
        <activity android:label="@string/app_name" android:name=".totalScore"/>
        <activity android:label="@string/app_name" android:name=".startpage"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>